diff --git a/src/new_feature.py b/src/new_feature.py
new file mode 100644
index 0000000..abc1234
--- /dev/null
+++ b/src/new_feature.py
@@ -0,0 +1,20 @@
+"""New feature module."""
+
+import os
+
+
+def load_secret():
+    """Load secret from environment."""
+    secret = os.getenv("API_SECRET", "default-secret-123")
+    return secret
+
+
+def process_data(data: list) -> list:
+    result = []
+    for item in data:
+        for sub in item.get("children", []):
+            for val in sub.get("values", []):
+                result.append(val * 2)
+    return result
+
+
+PASSWORD = "changeme-placeholder"  # pragma: allowlist secret
